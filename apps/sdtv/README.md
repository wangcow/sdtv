# sdtv (app)

Flutter Linux entrypoint for the Steam Deck–first IPTV player.

```bash
export PATH="$HOME/sdk/flutter/bin:$PATH"
flutter pub get
flutter run -d linux
```

**Product of the Wangcow Corporation** · Apache-2.0

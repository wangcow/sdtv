name: sdtv_player
description: media_kit / libmpv playback for sdtv.
version: 0.2.0
publish_to: none
repository: https://github.com/wangcow/sdtv

environment:
  sdk: ^3.8.0
  flutter: ">=3.22.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_video: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

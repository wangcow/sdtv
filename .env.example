# Local only — copy to .env.local (gitignored). Never commit real values.
# Use only when you deliberately test against your own paid provider.
#
# SDTV_XTREAM_URL=http://your-provider.example:8080
# SDTV_XTREAM_USER=
# SDTV_XTREAM_PASSWORD=
